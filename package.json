{
  "name": "figma-auto",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:bridge": "npm run build -w @figma-auto/protocol && npm run build -w @figma-auto/mcp-bridge",
    "build:plugin": "npm run build -w @figma-auto/figma-plugin",
    "build": "npm run build -w @figma-auto/protocol && npm run build -w @figma-auto/mcp-bridge && npm run build -w @figma-auto/figma-plugin",
    "dev:bridge": "sh scripts/start-local-bridge.sh --skip-build",
    "paths:local": "sh scripts/start-local-bridge.sh --print-config",
    "start:local": "sh scripts/start-local-bridge.sh",
    "test": "npm run test -w @figma-auto/protocol && npm run test -w @figma-auto/mcp-bridge"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.1",
    "@figma/plugin-typings": "^1.117.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}
